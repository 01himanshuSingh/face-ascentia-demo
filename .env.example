# PostgreSQL (local development via Docker Compose)
POSTGRES_DB=face_auth
POSTGRES_USER=face_auth
POSTGRES_PASSWORD=change_me_local_only
POSTGRES_PORT=5432
POSTGRES_HOST=localhost

# Backend
DATABASE_URL=postgresql+psycopg://face_auth:change_me_local_only@localhost:5432/face_auth
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# SDK / test-harness (browser)
VITE_API_BASE_URL=http://localhost:8000
